#include <bits/stdc++.h>

using ll = unsigned long long;
using namespace std;

int main()
{

    ios::sync_with_stdio(0);
    cin.tie(nullptr);

    ll t;
    cin >> t;
    while (t--)
    {
        ll row, col;
        cin >> row >> col;

        ll n = max(row, col);
        ll base = n * n;

        
    }

    return 0;
}